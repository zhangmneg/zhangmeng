#include <iostream>
#include <iomanip>
#include <array>
using namespace std;
int main()
{
    //a
    int SIZE=5;
    unsigned int values[SIZE]={2,4,6,8,10};
    //b
    unsigned int*vPtr;
    //c
    for(int i=i;i<=10;i=i+2)values[i-1]=i;
    //d
    unsigned int*vPtr=shan;
    vPtr=&values;
    //e
    cout<<fixed<<showpoint<<setprecisision(1);
    for(size_t k=0;k<size;++k)
        cout<<*(vPtr+j)<<' ';
    //f
    cout<<fixed<<showpoint<<setprecisision(1);
    for(size_t k=0;k<size;++k)
        cout<<*(values+k)<<' ';
    //g
    cout<<fixed<<showpoint<<setprecisision(1);
    for(size_t m=0;m<size;++m)
        cout<<vPtr[m]<<' ';
    //h
    values[4]
    *(values+4)
    vPtr[4]
    *(vPtr+4)
    //i
    "vPtr+3引用的地址是1002500+2*4=1002508，这个值存放值是8"
    //J
    "values[4]的地址是1002500+4*2=1002508，执行values[4]后地址是1002500-4*2=1002492，在该地址存放的值是2"

}
